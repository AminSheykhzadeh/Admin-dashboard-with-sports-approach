function navigate(page) {
  // This function should handle the navigation between different "pages" of your SPA
  // For now, it will just console log the page to navigate to
  console.log('Navigating to:', page);
}

document.addEventListener('DOMContentLoaded', () => {
  // Load your highcharts and other scripts here
});
